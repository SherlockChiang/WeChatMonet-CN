# 更新日志 / Changelog

所有显著变更记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [26S4-CN4] - 2026-08-29

### 修复
- **纯色底栏渲染成黑色**：原版 SolidTab 把 `color/bb` 固定为 `system_surface_dark`（恒黑）。现改为日间 `system_surface_container_light`、夜间 `system_surface_container_dark`（均为框架动态引用，跟随壁纸取色），真机验证 `bb → #fff0ecf6`。
- **模糊底栏与纯色底栏无区别**：大陆版主底栏走 `color/bb` 槽位（`df` 仅被单个次要布局引用），原模糊生成器只写 `df` 因此看不到效果。生成器现同时写入 `bb`（69%/78% 半透明 Monet 色）与 `df`——纯色=不透明、模糊=半透明，可区分。

### 注意
- 更新本版本后若要切换模糊/纯色，请通过模块 action 菜单（音量-）重新选择；夜间模式下两种底栏均为深色系（Monet 暗面），属预期行为。

## [26S4-CN3] - 2026-08-29

### 修复
- **消息长按菜单 / 朋友圈详情弹窗白底白图标**：大陆版有一组「永远深色」的弹窗/蒙层专用槽位（`y4`、`ub`、`y2`、`k3`、`t`、`e`、`l`、`qz`、`k`、`akl`、`akw`、`jz`、`n0`），其日间 stock 值即半透明黑/深灰，此前被套用了浅色 Monet 值。现已全部恢复微信原值（如 `y4`=#80000000、`e`=#ff4c4c4c/#ff404040），深色弹窗与白色图标恢复正常。
- 修复重建流水线的引用闭包误删 `values-night/colors.xml` 的问题（`@android:` 框架引用不再被当作缺失资源）。

## [26S4-CN2] - 2026-08-29

### 修复
- **联系人/群聊名称被染色**：回滚 `color/b3w → BW_0_Alpha_0_9`、`color/aow → zb`、`color/awp → a45` 三条重命名（大陆版这些名字是主文字/scrim 槽位，语义与 Play 不同），主文字恢复微信原色 #E6000000。
- 语义漂移复位：`color/ahk`（白）、`color/tw`（微信品牌绿 #FF07C160）恢复微信原值，绿色系按钮/指示器不再变色。

### 变更
- overlay 改为 **aapt2 全量重建**：从设备提取日/夜双配置解析值生成 `res/` 后编译，产出标准 RRO 结构；保留 `@android:color/system_*` 动态引用（跟随壁纸取色）。
- 关于页文案更新为「适配版本: 大陆版 8.0.76」。

### 兼容性
- **3141**（8.0.76 热修）：与 3140 逐字节对比 2160 颜色值、13843 资源文件零差异，门禁放行。
- **8.0.77 / 3160**：资源名洗牌（删 480 / 增 1172）但本模块全部条目零丢失；仅 10 处夜间深灰配置微调，浅色槽位零漂移，门禁放行。
- 大陆版自动跳过气泡样式安装与配置（安装器 + action.sh 双重门控）。

## [26S4-CN1] - 2026-08-28

### 新增
- 首个大陆版适配：基于 Play 8.0.72 版 WeChat Monet Pro 26S4，用 Play 8.0.72 ↔ 大陆版 8.0.76 双包比对（文件哈希 / 颜色值对 / 锚点引用图投影）重映射资源名。
- 基础 Monet 覆盖 234 色 + 15 drawable + 6 文案；多场景圆角 `dq_ → aj9`；`dik → d_u`。
- 大陆版自动跳过气泡样式（图片气泡架构，shape 气泡资源不存在）。
